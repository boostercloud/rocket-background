import { JobOptions } from './job-options'

export const backgroundOptionsID = 'backgroundOptionsID'
export const DEFAULT_BATCH_SIZE = 10
export const DEFAULT_JOB_OPTIONS = {
  retryTimes: 10,
} as JobOptions

